import Layout from 'layout/DocLayout';

## ρV

> ρV - undefined project

Powered by Next.js & MDX.js .

export default ({ children }) => {
  return (
    <Layout style={{ marginTop: '2rem' }}>
      {children}
    </Layout>
  );
};
