import { Container } from '@nextui-org/react';

## ρV

> ρV - undefined project

Powered by Next.js & MDX.js .

export default ({ children }) => {
  return <Container style={{ marginTop: '2rem' }}>{children}</Container>;
};
